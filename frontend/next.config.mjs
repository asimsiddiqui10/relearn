/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // pdfjs-dist ships a worker we load from /public; nothing special needed here.
};

export default nextConfig;
